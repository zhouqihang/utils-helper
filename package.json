{
  "name": "utils",
  "version": "0.0.1",
  "description": "utils",
  "main": "index.js",
  "scripts": {
    "build": "npm run clear && tsc && tsc -m ES2015 --outDir ./es",
    "clear": "node ./del.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "typings": "./typings",
  "devDependencies": {
    "del": "^4.1.0",
    "typescript": "^3.4.1"
  }
}
